<div class="container-fluid">
  <div class="row">
    <div class="col-md-12">

    <div class="row">
      <div class="col-md-5">
        <h1>購入履歴</h1>
        <p>会員登録時のユーザー情報を閲覧することができます。</p>
        <p>現在の送料 500円</p>
      </div>
    </div>

    <table class="table table-bordered">
      <tr>
        <th>購入日</th>
        <th>送付先</th>
        <th>ステータス</th>
        <th>総計</th>
      </tr>

      <% current_user.payments.each do |payment| %>
          <tr>
            <th><%= link_to payment_path(payment.id) do %>
                  <%= payment.created_at.strftime('%Y/%m/%d') %>
                <% end %>
            </th>
            <th><%= payment.shipping_address %></th>
            <th><%= payment.order_status %></th>
            <th><% sum = 0 %>
              <% payment.purchase_products.each do |purchase_product| %>
              <% sum += purchase_product.price * purchase_product.quantity %>
                <% end %>
                <%= sum + 500 %>
            </th>
          </tr>
      <% end %>
    </table>

    </div>

  <div class="row">
    <div class="col-md-12">
      <div class="col-md-4"></div>
      <div class="col-md-4">
      <button type="button" class="btn btn-primary btn-lg center-block btn-block">戻る</button>
      </div>
      <div class="col-md-4"></div>
    </div>
  </div>

        <div class="row">
          <div class="col-md-12">
            <div class="col-md-4">
            </div>
            <div class="col-md-5">
              <%= paginate @payments, class: "paginate" %>
            </div>
            <div class="col-md-3">
            </div>
          </div>
        </div>


  </div>
</div>