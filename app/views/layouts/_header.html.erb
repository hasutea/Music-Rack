<nav class="navbar">
  <div class="container-fluid"> <!-- 流動レイアウト -->
    <div class="row">
      <div class="col-md-12">

        <div id="header-logo">
          <%= image_tag "logo.png" %>
        </div>

        <div class="collapse navbar-collapse" id="navmenu">
          <ul class="nav navbar-nav navbar-right">
            <!-- 後日pathとユーザー名修正 -->
            <% if user_signed_in? %>
              <p class="navbar-text navbar-left">ようこそ、<span><%= current_user.last_name %></span><span><%= current_user.first_name %></span>さん
              </p>
              <li><%= link_to "", user_cart_path(current_user), class: "glyphicon glyphicon-shopping-cart" %></li>
              <li class="dropdpwn">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button">マイページ<span class="caret"></span></a>
                  <ul class="dropdown-menu" role="menu">
                    <li><%= link_to "登録内容確認", user_path(current_user) %></li>
                    <li><%= link_to "購入履歴一覧", payments_path %></li>
                  </ul>
              </li>
              <li><%= link_to "商品一覧", products_path %></li>
              <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>
            <% else %>
              <li><%= link_to "新規登録", new_user_registration_path %></li>
              <li><%= link_to "ログイン", new_user_session_path %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</nav>
<div class="main-visual">
  <div class= "main-visual-content">
    <p class= "main-visual-text">
      Welcome To Our Music Ruck<br>
      It's Nice To Meet You
    </p>
  </div>
</div>